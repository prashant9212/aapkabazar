import { SkipOtpDirective } from './skip-otp.directive';

describe('SkipOtpDirective', () => {
  it('should create an instance', () => {
    const directive = new SkipOtpDirective();
    expect(directive).toBeTruthy();
  });
});
