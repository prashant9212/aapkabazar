import { CityGoogleAutocompleteDirective } from './city-google-autocomplete.directive';

describe('CityGoogleAutocompleteDirective', () => {
  it('should create an instance', () => { 
    const directive = new CityGoogleAutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
