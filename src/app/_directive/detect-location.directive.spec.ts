import { DetectLocationDirective } from './detect-location.directive';

describe('DetectLocationDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectLocationDirective();
    expect(directive).toBeTruthy();
  });
});
